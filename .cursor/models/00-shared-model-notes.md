# 00 — Shared — Model Notes

| Step | 00 shared |
|------|-----------|

General guidance for all phases:

- Prefer precise, incremental edits over large rewrites.
- Run `pytest` after test or implementation changes.
- Match existing Python style in `UnitConverter.py` until refactoring phase.
- Do not merge to `main` unless the user asks.

Phase-specific notes live in `models/01-spec-model-notes.md`, etc.
