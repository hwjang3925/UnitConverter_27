# 05 — Refactoring — Structure only, tests stay Green

Branch must be **`refactoring`**.

1. Confirm branch → `refactoring`
2. Split OCP/SRP: Parser, Registry, Converter, Formatter
3. `pytest` after each refactor — all Green
4. Export step 05 via `00-export-artifacts`

Do not change behavior or delete tests.
